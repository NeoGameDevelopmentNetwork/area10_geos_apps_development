; UTF-8 Byte Order Mark (BOM), do not remove!
;
; Area6510 (c) by Markus Kanet
;
; This file is used to document the source code, not for
; creating an executable program.
;

;--- GEOS-Klasse für SetupGDOS.
if LANG = LANG_DE
			c "SetupGDOSde V3.0"
endif
if LANG = LANG_EN
			c "SetupGDOSen V3.0"
endif
